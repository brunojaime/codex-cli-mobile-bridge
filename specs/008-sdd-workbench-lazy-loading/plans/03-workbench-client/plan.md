# Workbench Client

Update the Flutter Workbench package to prefer lazy endpoints.

- Load project summary first.
- Hydrate a selected spec on demand.
- Cache loaded spec details while the panel is open.
- Fall back to the legacy full snapshot on old backends.

