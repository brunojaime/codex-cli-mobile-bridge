# T026 Render Kanban board columns, cards, badges, counts, and empty/loading/error states

Status: planned

Render the board with stable columns, readable cards, source badges, confidence indicators, counts, empty states, loading states, and error recovery.
