# T008 Implement phase progress and ready/backlog ordering rules

Status: planned

Compute phase progress and card ordering from phase number, task number, dependencies, status, and stable source identity. Avoid nondeterministic ordering from filesystem traversal.
