# T042 Add regression coverage for scope index, spec filtering, job filtering, refresh, and polling

Status: done

Cover backend scope-index output and Flutter request routing so selecting a spec sends `spec_id`, selecting a Project Factory job sends `job_id`, and refresh/polling preserve the active scope.
