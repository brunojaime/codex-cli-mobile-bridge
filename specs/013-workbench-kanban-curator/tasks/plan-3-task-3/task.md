# T015 Observe Codex/session transcript or JSONL sources when available without requiring full transcript downloads

Status: done

Use bounded transcript windows, lazy-loaded chat APIs, or JSONL tails to detect recent activity. Avoid full transcript reads for normal refreshes.
