# T038 Add Workbench Kanban scope selector for All specs and individual specs

Status: done

Render a Kanban scope selector with `All specs` plus every project spec, pass the selected `spec_id` through load, refresh, and polling, and make the active scope clear in the board header.
