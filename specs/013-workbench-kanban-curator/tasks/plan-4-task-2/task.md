# T021 Implement Curator scheduler with debounce, interval, and active-scope gating

Status: planned

Run Curator after meaningful board deltas with debounce and on a periodic interval while a scope is active. Skip inactive scopes unless explicitly requested.
