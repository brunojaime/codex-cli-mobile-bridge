# T039 Add Kanban scope index API for workspace, spec, Project Factory, and generated workspace scopes

Status: done

Expose a reusable Kanban scope index endpoint so the Workbench can choose between all specs, individual specs, live Project Factory draft/job work, and generated workspaces without loading every board at once.
