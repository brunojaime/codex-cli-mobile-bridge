# T006 Implement SDD task parser integration for `tasks.md`, `tree.json`, metadata, and plan phases

Status: done

Reuse existing SDD parsing paths where possible to load task status, task ordering, phase grouping, metadata state, and available files.
