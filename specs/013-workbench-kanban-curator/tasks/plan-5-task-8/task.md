# T041 Load Kanban scopes before board data and route selected scope parameters through the UI

Status: done

Load the Kanban scope index in the Flutter Workbench, render grouped scope options, and pass the selected workspace, spec, draft, or job parameters through initial load, manual refresh, history, and polling.
