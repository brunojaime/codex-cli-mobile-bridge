# Kanban Domain Contract Plan

Define stable scopes, cards, columns, evidence, and history payloads. This phase must make confirmed task state separate from inferred activity so the board can stay deterministic.
