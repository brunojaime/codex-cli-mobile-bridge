# T076 Add validation test that preview APK does not use localhost, placeholder, or mock primary data paths

Spec: `007-initial-preview-release`

Plan: `Validation And Regression Coverage`

## Checklist

- [ ] T076 Add validation test that preview APK does not use localhost,
      placeholder, or mock primary data paths.
