# T032 Fail preview validation when mock/local seeded data is the primary runtime

Spec: `007-initial-preview-release`

Plan: `Generated App Runtime Profile`

## Checklist

- [ ] T032 Fail preview validation when mock/local seeded data is the primary
      runtime.
