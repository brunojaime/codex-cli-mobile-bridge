# T008 Add a Cloudflare doctor service that validates account, zone, token scopes, D1 access, Pages access, Worker access, DNS record access, and optional R2 access

Spec: `006-web-preview-delivery`

Plan: `Cloudflare Configuration And Client Boundary`

## Checklist

- [x] T008 Add a Cloudflare doctor service that validates account, zone, token scopes, D1 access, Pages access, Worker access, DNS record access, and optional R2 access.
