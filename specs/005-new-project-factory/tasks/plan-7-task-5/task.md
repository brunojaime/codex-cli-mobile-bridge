# T027b Mark publication gaps as blocked with concrete command/log output instead of ready when GitHub origin, release APK, or Bridge registration is missing

Spec: `005-new-project-factory`

Plan: `GitHub And Release Readiness`

## Checklist

- [x] T027b Mark publication gaps as `blocked` with concrete command/log output
      instead of `ready` when GitHub origin, release APK, or Bridge registration
      is missing.
