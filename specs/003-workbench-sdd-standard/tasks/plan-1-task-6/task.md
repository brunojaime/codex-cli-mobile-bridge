# T006 Add compatibility rules to the loader: v1 is supported, v1.x is backward-compatible, unknown major versions are hard errors for write, context, indexing, and Codex action flows

Spec: `003-workbench-sdd-standard`

Plan: `Workbench SDD Standard`

## Checklist

- [x] T006 Add compatibility rules to the loader: v1 is supported, v1.x is
      backward-compatible, unknown major versions are hard errors for write,
      context, indexing, and Codex action flows.
