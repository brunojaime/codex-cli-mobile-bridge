# T061 Add prompt language that preserves project-owned domain rules and context overrides

Spec: `003-workbench-sdd-standard`

Plan: `LLM Operating Instructions`

## Checklist

- [x] T061 Add prompt language that preserves project-owned domain rules and
      context overrides.
