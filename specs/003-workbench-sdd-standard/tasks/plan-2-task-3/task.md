# T011 Define the sdd.context_rules schema for domain-to-module mappings, preferred context files, excluded paths, stricter candidate limits, and protected baseline overrides

Spec: `003-workbench-sdd-standard`

Plan: `Project Manifest Adoption`

## Checklist

- [x] T011 Define the `sdd.context_rules` schema for domain-to-module mappings,
      preferred context files, excluded paths, stricter candidate limits, and
      protected baseline overrides.
