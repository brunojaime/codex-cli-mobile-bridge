# T038 Add Mermaid syntax/render validation where the configured renderer is available; report failures as validation output without crashing

Spec: `003-workbench-sdd-standard`

Plan: `Diagram Taxonomy And Governance`

## Checklist

- [x] T038 Add Mermaid syntax/render validation where the configured renderer is
      available; report failures as validation output without crashing.
