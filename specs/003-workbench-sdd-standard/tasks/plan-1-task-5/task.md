# T005 Define in the standard artifact: artifact taxonomy, spec lifecycle, required metadata, diagram taxonomy, traceability schema, context pack names, index schemas, and LLM rules

Spec: `003-workbench-sdd-standard`

Plan: `Workbench SDD Standard`

## Checklist

- [x] T005 Define in the standard artifact: artifact taxonomy, spec lifecycle,
      required metadata, diagram taxonomy, traceability schema, context pack
      names, index schemas, and LLM rules.
