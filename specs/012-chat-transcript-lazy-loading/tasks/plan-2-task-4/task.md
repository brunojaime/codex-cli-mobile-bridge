# T008 Add paging metadata to session detail responses

Status: planned
Plan: Backend Pagination

Return `has_older`, cursor fields, `window_anchor_message_id`, and `is_partial` so the frontend can load older messages safely.

