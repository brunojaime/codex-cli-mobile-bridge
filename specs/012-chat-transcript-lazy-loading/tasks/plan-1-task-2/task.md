# T002 Define stable cursor format and ordering contract

Status: done
Plan: Message Window Contract

Define a cursor based on stable message ordering, preferably `created_at` plus `id`, so pagination remains correct when new messages are appended.

