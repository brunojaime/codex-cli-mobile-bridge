# T015 Deduplicate overlapping pages and live appended messages

Status: done
Plan: Frontend Lazy Thread

Ensure the message list remains unique when old pages overlap, polling refreshes state, or new messages append while pagination is active.

