# T001 Define transcript window response schema

Status: done
Plan: Message Window Contract

Define the fields returned by bounded transcript responses: visible messages, paging metadata, anchor message id, partial flag, and loaded-window boundaries.

