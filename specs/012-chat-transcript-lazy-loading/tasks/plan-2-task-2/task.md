# T006 Add repository/service method for latest user anchor window

Status: planned
Plan: Backend Pagination

Add service logic that finds the latest user-authored message and returns it with all later messages, with safety metadata if the window is capped.

