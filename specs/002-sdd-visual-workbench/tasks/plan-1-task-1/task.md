# Add a dev-mode workbench entry point in MaterialApp.builder

Spec: `002-sdd-visual-workbench`

Plan: `SDD Explorer`

## Checklist

- [x] Add a dev-mode workbench entry point in `MaterialApp.builder`.
