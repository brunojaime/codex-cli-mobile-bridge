# Layout Anchors And Routing Engine

Define how semantic diagrams are normalized into visual nodes, positioned automatically, connected through anchors, routed through paths, and updated after manual movement.

Required outcomes:

1. Connections are expanded into consumer, interface, provider visual structures.
2. Layout supports deterministic left to right placement and pinned node preservation.
3. Lines are routed from declared anchors and dynamic ports, not arbitrary points.
