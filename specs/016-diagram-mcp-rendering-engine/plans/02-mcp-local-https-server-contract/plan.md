# MCP Local HTTPS Server Contract

Define the local MCP Server that exposes diagram tools over HTTPS on localhost and acts as the integration surface for the agent.

Required outcomes:

1. The MCP Server has a clear localhost HTTPS configuration contract.
2. Tools are defined for creating, validating, updating, moving, rendering, and exporting diagrams.
3. Security boundaries, origin checks, auth expectations, and error behavior are specified.
