# Plan

1. Add the project SDD contract and architecture docs.
2. Add a dedicated backend service for read-only SDD snapshots.
3. Wire the service through the app container and router.
4. Add a read-only SDD doctor script for workspaces.
5. Add the minimal frontend dev-mode wrapper flag.
6. Validate with focused backend and frontend tests.
