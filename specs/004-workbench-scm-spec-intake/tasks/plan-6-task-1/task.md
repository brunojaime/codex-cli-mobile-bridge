# T023 Add backend/app/application/services/sdd_codex_job_service.py with job model, job directory, target workspace, context pack id, and state machine

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Orchestration`

## Checklist

- [x] T023 Add `backend/app/application/services/sdd_codex_job_service.py` with
      job model, job directory, target workspace, context pack id, and state
      machine.
