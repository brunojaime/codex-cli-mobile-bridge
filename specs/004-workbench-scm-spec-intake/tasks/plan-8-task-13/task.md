# T079 Add true pixel-crop generation in Workbench so a selected rectangle produces a separate cropped image artifact through the staged-media path, with backend/client tests proving the crop file is persisted end-to-end

Spec: `004-workbench-scm-spec-intake`

Plan: `Workbench UX For Specs`

## Checklist

- [x] T079 Add true pixel-crop generation in Workbench so a selected rectangle
      produces a separate cropped image artifact through the staged-media path,
      with backend/client tests proving the crop file is persisted end-to-end.
