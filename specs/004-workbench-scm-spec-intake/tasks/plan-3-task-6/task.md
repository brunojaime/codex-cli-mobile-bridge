# T068 Add safe media persistence in backend/app/application/services/sdd_intake_service.py and tests for text/audio/image/crop/region/batch/sequence payload refs, sha256 verification, no overwrite, cleanup on failure, and job handoff references

Spec: `004-workbench-scm-spec-intake`

Plan: `Multimodal Intake Storage`

## Checklist

- [x] T068 Add safe media persistence in
      `backend/app/application/services/sdd_intake_service.py` and tests for
      text/audio/image/crop/region/batch/sequence payload refs, sha256
      verification, no overwrite, cleanup on failure, and job handoff
      references.
