# T034 Refresh traceability and .sdd indexes after successful create/edit actions through existing index/backfill boundaries

Spec: `004-workbench-scm-spec-intake`

Plan: `Metadata Refresh`

## Checklist

- [x] T034 Refresh traceability and `.sdd` indexes after successful create/edit
      actions through existing index/backfill boundaries.
