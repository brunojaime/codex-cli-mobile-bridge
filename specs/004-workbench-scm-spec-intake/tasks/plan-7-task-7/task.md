# T035 Add tests/test_sdd_metadata_refresh_service.py for title/description refresh, pinned field conflict output, stale detection, idempotency, task summaries, traceability refresh, fresh indexes, and failed refresh next actions

Spec: `004-workbench-scm-spec-intake`

Plan: `Metadata Refresh`

## Checklist

- [x] T035 Add `tests/test_sdd_metadata_refresh_service.py` for title/description
      refresh, pinned field conflict output, stale detection, idempotency,
      task summaries, traceability refresh, fresh indexes, and failed refresh
      next actions.
