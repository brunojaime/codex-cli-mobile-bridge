# T044 Extend existing feedback/capture backend payload schemas with spec_target while preserving backward compatibility

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Bridge Spec Targeting`

## Checklist

- [x] T044 Extend existing feedback/capture backend payload schemas with
      `spec_target` while preserving backward compatibility.
