# T008 Add tests/test_sdd_spec_metadata.py for valid metadata, missing metadata fallback, malformed metadata warnings, stale metadata flags, and task summary parsing

Spec: `004-workbench-scm-spec-intake`

Plan: `SCM Metadata Model`

## Checklist

- [x] T008 Add `tests/test_sdd_spec_metadata.py` for valid metadata, missing
      metadata fallback, malformed metadata warnings, stale metadata flags, and
      task summary parsing.
