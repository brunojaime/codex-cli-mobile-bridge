# T074 Add structured media-derived intake support before Bridge capture: validate staged parent/child references for crop, marked region, screenshot batch, and image+audio sequence items; reject missing/deleted refs, invalid bounds, duplicate refs, and invalid ordering; persist structured metadata through intake storage/job handoff; expose host-injected structured attachments in Workbench without native drawing/crop UI

Spec: `004-workbench-scm-spec-intake`

Plan: `Workbench UX For Specs`

## Checklist

- [x] T074 Add structured media-derived intake support before Bridge capture:
      validate staged parent/child references for crop, marked region,
      screenshot batch, and image+audio sequence items; reject missing/deleted
      refs, invalid bounds, duplicate refs, and invalid ordering; persist
      structured metadata through intake storage/job handoff; expose
      host-injected structured attachments in Workbench without native
      drawing/crop UI.
