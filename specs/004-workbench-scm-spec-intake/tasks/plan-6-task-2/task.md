# T024 Implement validated Codex CLI process launch using argv command construction, resolved cwd, allowlisted env, prompt/context files, and no shell interpolation

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Orchestration`

## Checklist

- [x] T024 Implement validated Codex CLI process launch using argv command
      construction, resolved cwd, allowlisted env, prompt/context files, and no
      shell interpolation.
