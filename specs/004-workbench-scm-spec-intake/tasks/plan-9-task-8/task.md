# T077 Add Flutter Bridge capture UI tests for SDD target selector states, none preserving ordinary batch submission, new_spec preview/apply, existing_spec picker/job enqueue, backend blocked-state display, and no real Codex execution. Native crop/drawing UI remains pending

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Bridge Spec Targeting`

## Checklist

- [x] T077 Add Flutter Bridge capture UI tests for SDD target selector states,
      `none` preserving ordinary batch submission, `new_spec` preview/apply,
      `existing_spec` picker/job enqueue, backend blocked-state display, and
      no real Codex execution. Native crop/drawing UI remains pending.
