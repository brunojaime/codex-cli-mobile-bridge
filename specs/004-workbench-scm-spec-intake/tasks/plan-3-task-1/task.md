# T009 Define SddIntakeRequest/SddIntakeItem schemas in backend/app/api/schemas.py for text, audio, image, crop, marked region, screenshot batch, and image sequence inputs

Spec: `004-workbench-scm-spec-intake`

Plan: `Multimodal Intake Storage`

## Checklist

- [x] T009 Define `SddIntakeRequest`/`SddIntakeItem` schemas in
      `backend/app/api/schemas.py` for text, audio, image, crop, marked region,
      screenshot batch, and image sequence inputs.
