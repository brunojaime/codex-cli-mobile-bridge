# T032 Respect user_pinned_title and user_pinned_description; never overwrite pinned fields without explicit confirmation

Spec: `004-workbench-scm-spec-intake`

Plan: `Metadata Refresh`

## Checklist

- [x] T032 Respect `user_pinned_title` and `user_pinned_description`; never
      overwrite pinned fields without explicit confirmation.
