# T022 Add tests/test_sdd_spec_edit_service.py for invalid spec id, missing spec root, invalid artifact, path traversal, unsupported artifact, safe selected artifact targeting, and no files written on failed validation

Spec: `004-workbench-scm-spec-intake`

Plan: `Backend Existing Spec Edit Boundary`

## Checklist

- [x] T022 Add `tests/test_sdd_spec_edit_service.py` for invalid spec id,
      missing spec root, invalid artifact, path traversal, unsupported artifact,
      safe selected artifact targeting, and no files written on failed
      validation.
