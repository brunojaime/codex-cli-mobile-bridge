# T025 Build context packs for new_spec and existing_spec jobs through SddContextPackService without duplicating routing logic

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Orchestration`

## Checklist

- [x] T025 Build context packs for `new_spec` and `existing_spec` jobs through
      `SddContextPackService` without duplicating routing logic.
