# T001 Add SpecTargetRequest/response schemas in backend/app/api/schemas.py for none, new_spec, and existing_spec

Spec: `004-workbench-scm-spec-intake`

Plan: `Spec Target Contract`

## Checklist

- [x] T001 Add `SpecTargetRequest`/response schemas in
      `backend/app/api/schemas.py` for `none`, `new_spec`, and `existing_spec`.
