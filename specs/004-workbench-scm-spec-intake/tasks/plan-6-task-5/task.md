# T027 Capture stdout, stderr, exit code, timestamps, timeout, cancellation, touched file summary, and redacted argv in machine-readable job output

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Orchestration`

## Checklist

- [x] T027 Capture stdout, stderr, exit code, timestamps, timeout, cancellation,
      touched file summary, and redacted argv in machine-readable job output.
