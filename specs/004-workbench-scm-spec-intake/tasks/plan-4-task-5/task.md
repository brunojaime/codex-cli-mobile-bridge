# T018 Add tests/test_sdd_spec_creation_service.py for text-only new spec creation, dry-run before write, blocked collisions, no unintended writes, missing required intake, and strict doctor after creation

Spec: `004-workbench-scm-spec-intake`

Plan: `Backend Spec Creation Boundary`

## Checklist

- [x] T018 Add `tests/test_sdd_spec_creation_service.py` for text-only new spec
      creation, dry-run before write, blocked collisions, no unintended writes,
      missing required intake, and strict doctor after creation.
