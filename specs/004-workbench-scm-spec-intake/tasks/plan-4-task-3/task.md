# T016 Add API endpoint schemas/routes for new spec dry-run in backend/app/api/schemas.py and backend/app/api/routes.py; apply stays blocked until T017

Spec: `004-workbench-scm-spec-intake`

Plan: `Backend Spec Creation Boundary`

## Checklist

- [x] T016 Add API endpoint schemas/routes for new spec dry-run in
      `backend/app/api/schemas.py` and `backend/app/api/routes.py`; apply stays
      blocked until T017.
