# T067 Record that implementation remains blocked until the first implementation iteration is explicitly started

Spec: `004-workbench-scm-spec-intake`

Plan: `SAT Pilot And Reviewer Closeout`

## Checklist

- [x] T067 Record that implementation remains blocked until the first
      implementation iteration is explicitly started.
