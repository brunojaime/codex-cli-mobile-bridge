# T004 Add tests/test_sdd_spec_target.py covering valid targets, invalid combinations, Bridge/Workbench target conflicts, invalid spec IDs, path traversal, absolute paths, and unsupported artifact targets

Spec: `004-workbench-scm-spec-intake`

Plan: `Spec Target Contract`

## Checklist

- [x] T004 Add `tests/test_sdd_spec_target.py` covering valid targets, invalid
      combinations, Bridge/Workbench target conflicts, invalid spec IDs, path
      traversal, absolute paths, and unsupported artifact targets.
