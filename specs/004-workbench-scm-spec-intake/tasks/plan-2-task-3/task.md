# T007 Add metadata fallback behavior: title from heading, description from indexed spec summary, task counts from checkbox parsing in tasks.md

Spec: `004-workbench-scm-spec-intake`

Plan: `SCM Metadata Model`

## Checklist

- [x] T007 Add metadata fallback behavior: title from heading, description from
      indexed spec summary, task counts from checkbox parsing in `tasks.md`.
