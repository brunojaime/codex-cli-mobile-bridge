# T028 Add tests/test_sdd_codex_job_service.py covering target workspace validation, command injection prevention, cwd/env handling, timeout, cancellation, Codex CLI non-zero exit, concurrent submissions for one workspace, context pack use, and no broad all-spec fallback

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Orchestration`

## Checklist

- [x] T028 Add `tests/test_sdd_codex_job_service.py` covering target workspace
      validation, command injection prevention, cwd/env handling, timeout,
      cancellation, Codex CLI non-zero exit, concurrent submissions for one
      workspace, context pack use, and no broad all-spec fallback.
