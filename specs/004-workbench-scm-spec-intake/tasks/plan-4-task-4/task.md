# T017 Add explicit apply/write flow that consumes the dry-run plan and writes only inside the new spec root plus .sdd/

Spec: `004-workbench-scm-spec-intake`

Plan: `Backend Spec Creation Boundary`

## Checklist

- [x] T017 Add explicit apply/write flow that consumes the dry-run plan and
      writes only inside the new spec root plus `.sdd/`.
