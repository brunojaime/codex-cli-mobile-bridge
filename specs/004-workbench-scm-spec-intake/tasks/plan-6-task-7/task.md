# T069 Isolate Codex CLI execution in a per-job sandbox under .codex-bridge/sdd-jobs/<job-id>/sandbox, with handoff files and copied context artifacts, so Codex cannot modify target spec files directly by normal cwd-based writes

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Orchestration`

## Checklist

- [x] T069 Isolate Codex CLI execution in a per-job sandbox under
      `.codex-bridge/sdd-jobs/<job-id>/sandbox`, with handoff files and copied
      context artifacts, so Codex cannot modify target spec files directly by
      normal cwd-based writes.
