# T006 Extend SddWorkbenchViewService feature spec output with description, timestamps, generated/pinned flags, task summary, and last run state

Spec: `004-workbench-scm-spec-intake`

Plan: `SCM Metadata Model`

## Checklist

- [x] T006 Extend `SddWorkbenchViewService` feature spec output with
      description, timestamps, generated/pinned flags, task summary, and last
      run state.
