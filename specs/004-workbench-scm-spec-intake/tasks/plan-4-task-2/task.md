# T015 Add deterministic id/slug generation with collision handling and tests for duplicate requested slugs

Spec: `004-workbench-scm-spec-intake`

Plan: `Backend Spec Creation Boundary`

## Checklist

- [x] T015 Add deterministic id/slug generation with collision handling and
      tests for duplicate requested slugs.
