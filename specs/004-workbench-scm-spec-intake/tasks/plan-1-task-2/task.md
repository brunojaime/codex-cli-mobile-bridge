# T002 Add artifact target enum support for auto, spec, plan, tasks, and diagram in the same schema boundary

Spec: `004-workbench-scm-spec-intake`

Plan: `Spec Target Contract`

## Checklist

- [x] T002 Add artifact target enum support for `auto`, `spec`, `plan`,
      `tasks`, and `diagram` in the same schema boundary.
