# T021 Add API endpoint schemas/routes for existing spec edit dry-run and apply preparation in backend/app/api/schemas.py and backend/app/api/routes.py

Spec: `004-workbench-scm-spec-intake`

Plan: `Backend Existing Spec Edit Boundary`

## Checklist

- [x] T021 Add API endpoint schemas/routes for existing spec edit dry-run and
      apply preparation in `backend/app/api/schemas.py` and
      `backend/app/api/routes.py`.
