# T047 Route Bridge capture batches to the shared intake backend boundary instead of adding a Bridge-only creation path

Spec: `004-workbench-scm-spec-intake`

Plan: `Codex CLI Bridge Spec Targeting`

## Checklist

- [x] T047 Route Bridge capture batches to the shared intake backend boundary
      instead of adding a Bridge-only creation path.
